# 活动不捏造

近期活动一律以数据库为准，库里没有就回答没有，禁止 LLM 编造。

## 待办

- [ ] DeepSeek 工具调用里，`get_upcoming_events` 类工具的结果作为唯一可信来源。
- [ ] system prompt 里加硬约束：不得编造活动；空结果就直说没有。
- [ ] 加正则 / 关键词后置兜底：回复里如果出现"活动"+日期/时间词但工具未返回事件，标记可疑并拦截。

## 注意点

- LLM 单靠 prompt 不够稳，必须代码层做最终核验。

## 完成记录

- 2026-06-24：`sendMessage.js` 群分支始终注入活动上下文，空时显式给 `[本群近期活动: 无]`，并加 `ACTIVITY_GUARDRAIL` 硬约束（禁止编造时间/地点/参与者，查不到就说没有）。活动事实只来自 `eventStore`（按群文件）和 `get_upcoming_events` 工具，写操作已收归活动生命周期，LLM 无法直接造数据。
