# 群隔离

A 群的活动和上下文不能带到 B 群，画像注入也要避免跨群泄露。

## 待办

- [ ] 活动按群存（`eventStore.js` 已经是按群一文件，确认所有读路径都带 `roomName`）。
- [ ] 画像注入时，只把和当前群相关的 notes / tags 带进 prompt，跨群信息默认不进。
- [ ] 写一条最小验证：A 群消息触发的 prompt 不应出现 B 群的活动 / 备注。

## 注意点

- 这是项目红线，任何 LLM 输出都不能作为隔离判断依据，必须代码兜底。

## 完成记录

- 2026-06-24：活动已按群一文件（`eventStore.safeRoomKey`），读路径均带 `roomName`。画像 notes 增加来源 `group` 标记（`noteText`/`noteGroup` 兼容旧字符串），`formatProfileForPrompt(profile, scope)` 与 `isProfileRelevant(..., scope)` 只用当前群的 notes，跨群信息默认不进；`get_person_profile` 工具按 `roomName||'私聊'` 作用域。最小验证通过：B 群视角看不到 A 群 notes、相关性按群隔离、管理视图仍可看全量。
