# 日历

接入日历能力（农历 / 公历 / 节日 / 日期换算）。

## 待办

- [ ] 选库：`lunar-javascript` / `chinese-lunar-calendar` 一类。
- [ ] 加 function tool：`convert_date(date, from, to)` / `get_holidays(year?)`。
- [ ] 工作日 / 节假日要走当年官方安排（春节调休等），最好带可更新的数据源。

## 完成记录

- 2026-06-24：安装 `chinese-days`（国务院官方调休 + 农历 + 节气，全本地计算，零延迟）。新建 `src/platforms/wechat/commands/calendar.js`：`getDateInfo(date)` 和 `getHolidaySchedule(month)` 两个函数。`botTools.js` 加 `get_date_info` / `get_holiday_schedule` 工具，DeepSeek 按需调用。`/help` 更新。更新节假日数据：`npm update chinese-days`。
