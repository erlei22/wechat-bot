# 错误日志记录

捕获运行期异常并落库，便于后续修 bug。

## 实现

- `src/platforms/wechat/errorStore.js`：sqlite 存储，仿 `feedbackStore.js`。
- 各 catch 分支接入 `errorStore.logError(scope, error, context, dataDir)`，日志失败不影响主流程。
- 新增 `/错误` 管理指令：查看最近错误、按 scope 过滤、清空。

## 完成记录

- 已合入主线（具体 commit 待补）。
