# 天气预报

接入天气查询。

## 待办

- [ ] 选 API：和风、心知、QWeather、OpenWeatherMap，比较免费额度和中文地名支持。
- [ ] 加 function tool 给 DeepSeek 调用：`get_weather(city, date?)`。
- [ ] 缓存近期查询结果，避免高频重复调。
- [ ] 城市识别错误时给 "没找到这个城市" 而不是瞎编。

## 完成记录

- 2026-06-24：选用 **和风天气（QWeather）**，国内直连、免费 1000 次/天、3 天预报。新建 `src/platforms/wechat/commands/weather.js`：城市名 GeoAPI 解析 + 实时天气 + 3 天预报，30 分钟内存缓存，和风错误码中文提示，未配置 Key 时给注册引导。`botTools.js` 加 `get_weather(city, days?)` 工具定义和执行分支，DeepSeek 会自动在"上海明天天气"等问题时调用。`/help` 菜单加天气说明。未设 Key 时 fallback 正常，配置 `QWEATHER_API_KEY=你的Key` 后直接生效。
