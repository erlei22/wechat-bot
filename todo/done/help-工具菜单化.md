# `/help` 改为工具菜单

`/help` 切成固定词条菜单，直接列小工具，不再用 LLM 判断调用哪个方法。

## 现状

- `commandRouter.js` 的 `generateHelp` 已经改成纯代码生成菜单，不走 LLM。
- 菜单里列的：活动、画像、群分析、反馈。

## 待办

- [ ] 校对菜单条目和实际可用指令是否对齐（这次给 `/统计 群` 加权限后，菜单里 `<群名>` 参数要去掉）。
- [ ] 把不再暴露给普通用户的指令（比如 `/patterns`、`/错误`）从 help 里拿掉。

## 备注

这条已经基本完成主体改造，剩下的是和其他 todo 一起收尾的细节。

## 完成记录

- 2026-06-24：`generateHelp` 改为同步返回固定菜单字符串，移除 `getDeepseekReply` 调用与 import。菜单只列面向用户的小工具（活动 / 画像 / 群分析 / 反馈），未暴露 `/patterns`、`/错误` 等管理指令。配合「统计分析仅当前群」，菜单里的 `<群名>` 参数已去掉。
